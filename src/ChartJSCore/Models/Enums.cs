namespace ChartJSCore.Models
{
    public static class Enums
    {
        public enum ChartType : byte
        {
            BarChart = 0,
            BubbleChart = 1,
            RadarChart = 2,
            PolarAreaChart = 3,
            PieChart = 4
        }
    }
}
